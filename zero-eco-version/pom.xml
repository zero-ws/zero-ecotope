<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>zero-ecotope</artifactId>
        <groupId>io.zerows</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>zero-eco-version</artifactId>
    <packaging>pom</packaging>
    <name>Zero.Development.Kit.${project.version}</name>
    <dependencyManagement>
        <dependencies>
            <!--
                新版依赖
                zero-epic       / zero-ams
                zero-elite      / zero-metadata
                                / zero-co
                                / zero-macro
                zero-energy     / zero-web
                                / zero-osgi
                zero-equip        按需选择
             -->
            <!-- OSGI -->
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-ams</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-core-runtime-metadata</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-core-runtime-domain</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-core-cloud-environment</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-osgi-development-kit</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Library -->
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-co</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-macro</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-rx</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-web</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Zero Infix 插件 -->
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-equip</artifactId>
                <version>${project.version}</version>
            </dependency>
            <!-- Zero Extension -->
            <dependency>
                <groupId>io.zerows</groupId>
                <artifactId>zero-ke</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>