# Backend Development Guide

> Legacy entry document retained as a navigation page.
> Use the focused backend documents below instead of treating this file as the primary rule body.

## 1. Use These Files Instead

| Need | Document |
|---|---|
| DPA structure and async backend contract | `backend-dpa-guide.md` |
| DBE, `QQuery`, `QTree`, query placement | `dbe-query-rules.md` |
| table conventions, model metadata, tenant rules | `backend-model-tenant-rules.md` |
| RBAC resources and declarative permission rules | `backend-rbac-rules.md` |
| CRUD-engine-first decision model | `crud-engine-guide.md` |

## 2. Legacy Role

This file now exists only to preserve old references.

Rule:

```text
Do not expand this file again. Add or update the focused backend documents instead.
```
