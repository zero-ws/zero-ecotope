# Exmodule Report Guide

> Load this file when the task is about configurable report center behavior, report instances, or reusable reporting semantics.

## 1. Scope

This file owns `zero-exmodule-report`.

## 2. Key Anchors

- `ReportActor`
- `ReportStub`
- `ReportInstanceStub`
- `ReportService`
- `ReportInstanceService`
- `ExtensionReportSource`
- `MDReportActor`

## 3. AI Agent Rules

- Put configurable report-center semantics here.
- Keep monitor capability and generic export plumbing outside unless the issue is truly infrastructure-level.
