# Integration Guide

> Legacy entry document retained as a navigation page.
> Use the focused integration documents below instead of treating this file as the primary rule body.

## 1. Use These Files Instead

| Need | Document |
|---|---|
| `.r2mo` contract authority and source precedence | `contract-source-rules.md` |
| contract-aligned backend runtime API integration | `backend-api-integration.md` |
| Rust / Tauri / React client integration rules | `frontend-client-integration.md` |
| runtime identity and environment variable contracts | `environment-contracts.md` |

## 2. Legacy Role

This file now exists only to preserve old references.

Rule:

```text
Do not expand this file again. Add or update the focused integration documents instead.
```
