# Exmodule MBSE API Guide

> Load this file when the task is about dynamic API exposure, job-facing MBSE operations, or `zero-exmodule-mbseapi`.

## 1. Scope

This file owns `zero-exmodule-mbseapi`.

## 2. Key Anchors

- `TaskActor`
- `UriActor`
- `TaskApi`
- `JobStub`
- `JobService`
- `ExtensionMBSEApiSource`
- `MDMBSEApiActor`

## 3. AI Agent Rules

- Use this module when the problem is dynamic API or job management above the core job runtime.
- Keep raw `@Job` runtime concerns in `job-model-guide.md`.
