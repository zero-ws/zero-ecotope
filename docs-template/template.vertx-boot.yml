# 符号说明
# - 🪢 表示配置的值中包含了 ${} 的占位符解析文件引用
# - ---> 💻 表示配置的值中包含了环境变量信息
# 「云端配置」vertx-boot.yml ---------------------------------------------
# vertx:
#   config:
#     import:
#       - optional:nacos:r2mo-app-shared?refreshEnabled=true                # 此处 r2mo-app-shared 的配置必须存在于 Nacos 上，否则只提供下边的 optional 即可
#       - optional:nacos:${vertx.application.name}?refreshEnabled=true      # 🪢
#   application:
#     name: ${Z_APP:r2mo-app-momo}                                          # ---> 💻 Z_APP / 微服务名称、应用名称
#
# ---
# 「云端配置」分段 --------------------------------------------------------
# vertx:
#   cloud:
#     nacos:
#       discovery:
#         server-addr: ${vertx.cloud.nacos.server-addr}                     # 🪢
#         namespace: ${vertx.cloud.nacos.config.namespace}                  # 🪢
#       config:
#         server-addr: ${vertx.cloud.nacos.server-addr}                     # 🪢
#         namespace: ${R2MO_NACOS_NS_APP}                                   # ---> 💻️ R2MO_NACOS_NS_APP / Nacos 命名空间
#         prefix: ${vertx.application.name}                                 # 🪢 Nacos 配置前缀
#         file-extension: yaml                                              # Nacos 配置文件格式
#       server-addr: ${R2MO_NACOS_ADDR:localhost:8848}                      # ---> 💻️ R2MO_NACOS_ADDR / Nacos 地址直接指定
#       username: ${R2MO_NACOS_USER:nacos}                                  # ---> 💻️ R2MO_NACOS_USER / Nacos 用户名
#       password: ${R2MO_NACOS_PASS:nacos}                                  # ---> 💻️ R2MO_NACOS_PASS / Nacos 密码
#       name: ${vertx.application.name}                                     # 🪢 Nacos 名称
