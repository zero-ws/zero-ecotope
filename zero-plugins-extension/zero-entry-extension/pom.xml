<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>zero-plugins-extension</artifactId>
        <groupId>io.zerows</groupId>
        <version>1.0.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>zero-entry-extension</artifactId>
    <name>Zero.Core.Entry.Extension</name>

    <dependencies>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-resolver-dns-native-macos</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <!-- 入口依赖 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-entry-facade</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Extension 依赖 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-commerce-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 新版模块 ================================= -->

        <!-- CRUD统一模块 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-runtime-crud</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 集成专用 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-runtime-integration</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 文档考试管理 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-commerce-document</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 模块化专用 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-mbse-modulat</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- User Interface 专用模块 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-mbse-ui</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- API -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-mbse-action</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- PSI -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-commerce-psi</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- WF -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-runtime-workflow</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- FM -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-commerce-finance</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 动态建模引擎 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-mbse-basement</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 图引擎 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-runtime-graphic</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Tpl -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-runtime-tpl</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 报表系统,Report -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-runtime-report</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- LBS -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-commerce-lbs</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 特殊模块 ==============================  -->
        <!-- History 专用包 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-common-trash</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- ElasticSearch API -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-store-elasticsearch</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Command 命令行工具 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-common-shell</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-swagger</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Stomp for MQ -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-websocket-stomp</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
</project>