data:
  keyword: "crud.ticket.by-key"
  resource: "resource.workflow"
  virtual: true
  seekSyntax:
    phase: AFTER
    data:
      type: RECORD
      viewId: "`${viewId}`"
      identifier: "w.ticket"
