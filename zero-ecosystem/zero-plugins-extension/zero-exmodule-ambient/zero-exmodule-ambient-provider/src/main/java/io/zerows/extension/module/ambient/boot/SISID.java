package io.zerows.extension.module.ambient.boot;

import io.zerows.specification.development.HMaven;

public interface SISID extends HMaven {
    String BUNDLE_SYMBOLIC_NAME = "zero-exmodule-ambient / ( Virtual )";
}
