<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>zero-boot</artifactId>
        <groupId>io.zerows</groupId>
        <version>1.0.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>zero-boot-mini-actor</artifactId>
    <name>✅️ - Zero::Boot::Mini Actor</name>
    <dependencies>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-epoch-adhoc</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- New -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-epoch-use</artifactId>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-epoch-execution</artifactId>
        </dependency>
        <!-- Zero Web Container -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-epoch-cosmic</artifactId>
        </dependency>


        <!-- Reflection -->
        <dependency>
            <groupId>cglib</groupId>
            <artifactId>cglib</artifactId>
        </dependency>
        <dependency>
            <groupId>org.ow2.asm</groupId>
            <artifactId>asm</artifactId>
        </dependency>
        <dependency>
            <groupId>org.reflections</groupId>
            <artifactId>reflections</artifactId>
        </dependency>
        <dependency>
            <groupId>org.javassist</groupId>
            <artifactId>javassist</artifactId>
        </dependency>


        <!-- io_uring -->
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-io_uring-incubator</artifactId>
        </dependency>


        <!-- 请求错误：无法访问目标地址，检查对口是否可被正常访问 -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-codec-http2</artifactId>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-handler-proxy</artifactId>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-resolver-dns</artifactId>
        </dependency>
        <!-- Import Stack -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-version</artifactId>
            <version>${zero.version}</version>
            <scope>import</scope>
            <type>pom</type>
        </dependency>
        <!-- 底层实现追加 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>r2mo-typed-vertx</artifactId>
        </dependency>
    </dependencies>
</project>
