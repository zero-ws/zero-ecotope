<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>zero-boot-actor-extra</artifactId>
        <groupId>io.zerows</groupId>
        <version>1.0.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>zero-boot-extension-actor</artifactId>
    <name>✅️ - Zero::Boot::Extension Actor</name>
    <dependencies>
        <!-- Actor -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-boot-test-actor</artifactId>
            <version>${zero.version}</version>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-boot-elastic-actor</artifactId>
            <version>${zero.version}</version>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-boot-graphic-actor</artifactId>
            <version>${zero.version}</version>
        </dependency>
        <!-- Inst -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-boot-inst-load</artifactId>
            <version>${zero.version}</version>
        </dependency>
    </dependencies>
</project>