<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.zerows</groupId>
        <artifactId>zero-boot</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>zero-boot-extension</artifactId>
    <name>🧬 - Zero::Boot::Extension</name>
    <dependencies>
        <!--
        静态插件
        - trash
        - flyway
        - security
        -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-swagger</artifactId>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-trash</artifactId>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-websocket</artifactId>
        </dependency>
        <!-- 核心底层 -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-extension-api</artifactId>
        </dependency>
        <!-- ================= 静态插件 ================== -->
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-sms</artifactId>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-weco</artifactId>
        </dependency>
        <dependency>
            <groupId>io.zerows</groupId>
            <artifactId>zero-plugins-elasticsearch</artifactId>
        </dependency>
    </dependencies>
</project>